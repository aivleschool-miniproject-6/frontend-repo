version: 0.2

phases:
  install:
    commands:
      - echo "Install phase"

  pre_build:
    commands:
      - echo "Pre-build phase"
      - echo "Current files:"
      - ls -la

  build:
    commands:
      - echo "Build phase"
      - echo "CI pipeline is working"

  post_build:
    commands:
      - echo "Build completed successfully"